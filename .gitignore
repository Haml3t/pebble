# Pebble watchapp build output
build/
.lock-waf*
*.pbw

# Local recordings pulled from the phone by tools/audio-editor/server.py.
# Personal data — never commit.
tools/audio-editor/recordings/

# Pebble SDK / pebble-tool caches
.pebble-sdk/
*.pebble_screenshot_*.png
pebble_screenshot_*.png

# JS / npm
node_modules/

# Python virtualenvs (per-experiment)
.venv/
__pycache__/
*.pyc

# Android (Gradle outputs and caches)
android/.gradle/
android/.android-home/
android/app/build/
android/local.properties
.gradle-cache/

# Local secrets — anything you don't want public goes in a file matching these
secrets.local.*
*.local.json
*.local.html
.env
.env.*

# Editor / OS junk
.DS_Store
.idea/
.vscode/
*.swp
*~

# Claude Code project-local settings
.claude/
